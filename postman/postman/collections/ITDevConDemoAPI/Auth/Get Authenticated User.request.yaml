$kind: http-request
method: GET
url: '{{baseUrl}}/api/users-auth/{{id}}'
order: 2000
pathVariables:
  - key: id
    value: '1'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
